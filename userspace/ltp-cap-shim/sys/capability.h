#ifndef _SYS_CAPABILITY_H
#define _SYS_CAPABILITY_H
/* Stub for cross-compilation (matches the other arches' sysroot stub).
 * LTP's config.h was generated with HAVE_SYS_CAPABILITY_H, so tst_test.h
 * includes <sys/capability.h>; the Bootlin musl sysroot lacks it and the
 * MM tests link without -lcap, so a header stub is sufficient. */
#endif
